import 'package:flutter/material.dart';

class SelectionListTile extends StatelessWidget {
  const SelectionListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      title: Text("JamalScript"),
    );
  }
}
