import 'package:flutter/material.dart';

class MaterialContent extends StatelessWidget {
  const MaterialContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
