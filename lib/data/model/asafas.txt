// starting page

if nama:
    lanjut aplikasi
else
    login

// login.page
nama.input("nama anda")
setNama(nama)

// provider
nama = prefs.get(nama)
setnama(nama) {
    prefs.set(nama)
}

// provider.dart
final prefs = await SharedPreferences.getInstance();
class Userdata extends ChangeNotifier {
  String _name = prefs.getString('username);
  String? get name => _name;
  void setUserName(name) async {
    await prefs.setString('username', name);
  }
  ...
}

// starting.dart
nama = provider.of<UserData>(context).name
if (nama.isEmpty) {
    await navigator.pushNamed(context, "/login")
}
>>> kode selanjutnya untuk menjalankan aplikasi


// login.dart
...
    textField(...)
    ElevatedButton: () {
         provider.of<UserData>(context).setName("asep")
    }
